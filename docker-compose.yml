version: '3.8'

services:
  watchtower:
    container_name: watchtower
    image: applegater/watchtower:v1.0.1
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    env_file:
      - .env
